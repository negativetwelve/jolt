{
  "name": "jolt",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=8.0.0",
    "yarn": ">=0.27.5"
  },
  "devDependencies": {
    "eslint": "^4.4.1",
    "husky": "^0.14.3",
    "jest": "^20.0.4",
    "jest-addons": "^2.3.0",
    "lerna": "^2.0.0",
    "lint-staged": "^4.0.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.js": [
      "yarn lint"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "lint": "eslint packages",
    "precommit": "lint-staged",
    "test": "jest"
  }
}
