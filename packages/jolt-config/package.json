{
  "name": "jolt-config",
  "version": "2.8.5",
  "description": "Jolt config for the CLI.",
  "license": "MIT",
  "author": "Mark Miyashita <negativetwelve@gmail.com>",
  "homepage": "https://github.com/negativetwelve/jolt",
  "repository": "https://github.com/negativetwelve/jolt/tree/master/packages/jolt-config",
  "keywords": [
    "cli",
    "config",
    "development",
    "dx",
    "jolt"
  ],
  "main": "build/index",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "babel src --out-dir build --ignore '**/*.spec.js'",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
